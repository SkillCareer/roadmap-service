name: Service CI

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop, main]

jobs:
  ci:
    uses: SkillCareer/roadmap-service/.github/workflows/reusable-ci.yml@main
    with:
      node_version: "18"
      image_name: "roadmap-service"
    secrets: inherit
